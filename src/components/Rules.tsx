const Rules = () => {
  return (
    <div className="rules">
      <h3>How to Play</h3>
      <p>
        Take turns dropping chips into the columns. The first player to connect
        4 of their chips in a row (horizontally, vertically, or diagonally)
        wins.
      </p>
    </div>
  );
};

export default Rules;
